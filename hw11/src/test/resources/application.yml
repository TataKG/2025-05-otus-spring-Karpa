spring:
  data:
    mongodb:
      port: 0
      database: library_test
      host: localhost
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2

mongock:
  enabled: false